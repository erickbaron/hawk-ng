import { FormControl } from '@angular/forms';

export class ItemCompra {
    id: number;
    valorItem: number;
    quantidade: number;
    registroAtivo: boolean;
    produtoId: number;
    compraId: number;

}